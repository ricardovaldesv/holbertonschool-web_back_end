#!/usr/bin/env python3
""""A type-annotated function concat that takes a string str1 and a string
    str2 as arguments and returns a concatenated string.
"""


def concat(str1: str, str2: str) -> str:
    """Method that concat two string and return one string"""
    return (str1 + str2)
